apiVersion: v1
kind: Service
metadata:
  name: {{ template "admin-portal.fullname" . }}
  labels:
    app: {{ template "soldier-portal.name" . }}
    chart: {{ template "soldier-portal.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "soldier-portal.name" . }}
    release: {{ .Release.Name }}
